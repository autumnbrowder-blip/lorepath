"use client";

/**
 * Previously applied a desktop translate+scale on [data-scroll-parallax]
 * layers. That recropped/zoomed library photos on scroll and control clicks.
 * Kept as a no-op so layout.tsx does not change.
 */
export function ScrollParallax() {
  return null;
}
