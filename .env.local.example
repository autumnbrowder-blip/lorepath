# Supabase — copy this file to .env.local and fill in your project values.
# Find these in: Supabase Dashboard → Project Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-only — never prefix with NEXT_PUBLIC_. Used after JWT verification for
# preference/rating/profile writes (bypasses RLS). Find it in:
# Supabase Dashboard → Project Settings → API → service_role (secret)
# Also set this in Netlify → Site configuration → Environment variables.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional — comma-separated emails that may open /admin (server-only).
# Primary gate is profiles.is_admin; this bootstraps the first admin before/without SQL.
# Also set in Netlify → Site configuration → Environment variables, then redeploy.
# ADMIN_EMAILS=you@example.com

# Used for OAuth / email-confirmation redirects. Locally:
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# In Netlify Production, set this to:
#   NEXT_PUBLIC_SITE_URL=https://lorepath.net
# (Do not leave localhost in Production — it breaks non-reset auth emails.)

# Supabase Dashboard → Authentication → URL Configuration → Redirect URLs
# must include (at least):
#   http://localhost:3000/auth/callback
#   https://lorepath.net/auth/callback
# Password reset emails always use (hardcoded in app):
#   https://lorepath.net/auth/callback?next=/reset-password

# Optional — Google Books API key for higher rate limits
# GOOGLE_BOOKS_API_KEY=your-google-books-api-key

# Optional — New York Times Books API (bestsellers strip on Browse; search works without it)
# Free key: https://developer.nytimes.com/ → Get Started → Books API
# Also set this in Netlify → Site configuration → Environment variables for production.
# NYT_BOOKS_API_KEY=

# Optional — ISBNdb (https://isbndb.com/isbn-database)
# Also set in Netlify → Site configuration → Environment variables for production.
# ISBNDB_API_KEY=

# Optional — Big Book API (server-only; do NOT use NEXT_PUBLIC_)
# Free key: sign up at https://bigbookapi.com (free plan has a daily quota).
# The app sends it via the x-api-key header. Search works without it —
# Big Book simply contributes 0 results when unset.
# Also set in Netlify → Site configuration → Environment variables for
# Production (and Preview if needed), then redeploy.
# Restart `npm run dev` after changing this locally.
# BIG_BOOK_API_KEY=
